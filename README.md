# rust-perceptron

## Backend

### Build
`$ cd backend`
`$ rustup default nightly`
`$ rustup update && cargo update`
`$ cargo build`

### Run
`$ cargo run`

## Frontend

### Build
`$ vue create frontend`

### Run
`$ cd frontend`
`$ npm run serve`
